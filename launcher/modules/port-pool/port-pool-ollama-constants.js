/**
 *
 * @version 1.1.2 - March 5, 2026
 * @copyright 2026 Pseudo SF
 */
// Standard pools
const WEBUI_PORT_START = 52434;
const WEBUI_PORT_END = 52443;
const TOTAL_WEBUI_PORTS = WEBUI_PORT_END - WEBUI_PORT_START + 1;

const ANYTHINGLLM_PORT_START = 52444;
const ANYTHINGLLM_PORT_END = 52453;
const TOTAL_ANYTHINGLLM_PORTS = ANYTHINGLLM_PORT_END - ANYTHINGLLM_PORT_START + 1;

const TERMINAL_PORT_START = 52454;
const TERMINAL_PORT_END = 52463;
const TOTAL_TERMINAL_PORTS = TERMINAL_PORT_END - TERMINAL_PORT_START + 1;

// Enterprise pools
const AGENT_PORT_START = 53000;
const AGENT_PORT_END = 62999;
const TOTAL_AGENT_PORTS = AGENT_PORT_END - AGENT_PORT_START + 1;

const COORDINATOR_PORT_START = 63000;
const COORDINATOR_PORT_END = 63499;
const TOTAL_COORDINATOR_PORTS = COORDINATOR_PORT_END - COORDINATOR_PORT_START + 1;

const EXTERNAL_PORT_START = 63500;
const EXTERNAL_PORT_END = 63999;
const TOTAL_EXTERNAL_PORTS = EXTERNAL_PORT_END - EXTERNAL_PORT_START + 1;

// Totals
const TOTAL_STANDARD_PORTS = TOTAL_WEBUI_PORTS + TOTAL_ANYTHINGLLM_PORTS + TOTAL_TERMINAL_PORTS;
const TOTAL_ENTERPRISE_PORTS = TOTAL_AGENT_PORTS + TOTAL_COORDINATOR_PORTS + TOTAL_EXTERNAL_PORTS;
const TOTAL_PORTS = TOTAL_STANDARD_PORTS + TOTAL_ENTERPRISE_PORTS;

const AGENT_ROLES = {
  GATE_AND: 'gate-and',
  GATE_OR: 'gate-or',
  GATE_NOT: 'gate-not',
  GATE_XOR: 'gate-xor',
  COMPARATOR: 'comparator',
  CONDITIONAL: 'conditional',
  CLASSIFIER: 'classifier',
  SUMMARIZER: 'summarizer',
  EXTRACTOR: 'extractor',
  VALIDATOR: 'validator',
  EXPERT_CODE: 'expert-code',
  EXPERT_MATH: 'expert-math',
  EXPERT_SCIENCE: 'expert-science',
  EXPERT_LEGAL: 'expert-legal',
  EXPERT_MEDICAL: 'expert-medical',
  EXPERT_FINANCE: 'expert-finance',
  EXPERT_CREATIVE: 'expert-creative',
  ROUTER: 'router',
  AGGREGATOR: 'aggregator',
  DISPATCHER: 'dispatcher',
  SUPERVISOR: 'supervisor',
  SENSOR_READER: 'sensor-reader',
  ACTUATOR_WRITER: 'actuator-writer',
  SERIAL_BRIDGE: 'serial-bridge',
  MQTT_BRIDGE: 'mqtt-bridge',
  WEBSOCKET_BRIDGE: 'websocket-bridge',
  GPIO_BRIDGE: 'gpio-bridge',
  PLC_BRIDGE: 'plc-bridge',
  SBC_BRIDGE: 'sbc-bridge',
  WORKER: 'worker',
  CUSTOM: 'custom'
};

module.exports = {
  WEBUI_PORT_START,
  WEBUI_PORT_END,
  TOTAL_WEBUI_PORTS,
  ANYTHINGLLM_PORT_START,
  ANYTHINGLLM_PORT_END,
  TOTAL_ANYTHINGLLM_PORTS,
  TERMINAL_PORT_START,
  TERMINAL_PORT_END,
  TOTAL_TERMINAL_PORTS,
  AGENT_PORT_START,
  AGENT_PORT_END,
  TOTAL_AGENT_PORTS,
  COORDINATOR_PORT_START,
  COORDINATOR_PORT_END,
  TOTAL_COORDINATOR_PORTS,
  EXTERNAL_PORT_START,
  EXTERNAL_PORT_END,
  TOTAL_EXTERNAL_PORTS,
  TOTAL_STANDARD_PORTS,
  TOTAL_ENTERPRISE_PORTS,
  TOTAL_PORTS,
  AGENT_ROLES
};
